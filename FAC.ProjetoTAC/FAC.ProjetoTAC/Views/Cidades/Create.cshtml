@using FAC.ProjetoTAC.Models
@model Fabio.ProjetoTAC.Models.Cidade

@{
   
    //ConectDbContext ConectDbContext = new ConectDbContext();

    //var list = ConectDbContext.Estadoes.OrderBy(c => c.uf).ToList();
    
    //SelectList selectList = new SelectList(list,"iduf","uf",Model);
    
    
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Cidade</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.nome)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.nome)
            @Html.ValidationMessageFor(model => model.nome)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.idUF)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.idUF)
            @*@Html.DropDownListFor(model => model.idUF, selectList,"[Selecione]", new {@style = "color:#dae861"})*@
            @Html.ValidationMessageFor(model => model.idUF)
        </div>

        <p>
            <input type="submit" value="Gravar" />
        </p>
    </fieldset>
}

<div class="voltar">
    @Html.ActionLink("Voltar", "../Basic/")
</div>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
